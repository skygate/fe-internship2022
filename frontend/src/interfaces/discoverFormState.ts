export interface DiscoverFormState {
    category: string;
    time: string;
    sort: string;
    ascending: string;
    priceMin: number;
    priceMax: number;
    priceRangeMin: number;
    priceRangeMax: number;
}
