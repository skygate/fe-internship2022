export interface discoverFormState {
    category: string;
    time: string;
    sortBy: string;
    ascending: string;
    priceMin: number;
    priceMax: number;
    priceRangeMin: number;
    priceRangeMax: number;
}
